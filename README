This is workin progress

BUILD images:

  To build the images, use:

    make ge863_pro3_defconfig
    make

  Use the output/images/barebox.bin, output/images/uImage and
  output/images/rootfs.ubi files.

FLASH images:

  Replace the telit u-boot with barebox.bin:

    loadb
    protect off all
    cp.b 0x20200000 403C0000 $filesize
    reset

  After reset flash the kernel and rootfs using tftp:

    dhcp
    eth0.serverip=<tftp server ip adresss>
    update -d nand -t kernel
    update -d nand -t rootfs
    boot
